mod utility;
pub mod command_struct;

mod parsers;
pub mod packet_manager;

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
